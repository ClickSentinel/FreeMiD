{
  "name": "freemid-extension",
  "version": "0.4.0",
  "license": "GPL-3.0-or-later",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/build-activities.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "@types/node": "^25.9.3",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
